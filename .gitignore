# Dependencies
node_modules/

# 패키지 매니저는 yarn 만 사용 (npm 락파일 금지) — CLAUDE.md 참고
package-lock.json

# Build output — dist 는 커밋하지 않습니다.
# 소비 시스템이 git 의존성으로 설치하면 prepare 스크립트가 설치 시점에 빌드합니다.
# (docs/03-getting-started.md, docs/07-contributing.md)
dist/
dist-demo/

# Logs
*.log
npm-debug.log*

# Env / 로컬 설정 (비밀정보 커밋 금지)
.env
.env.*
!.env.example

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Test / coverage
coverage/
