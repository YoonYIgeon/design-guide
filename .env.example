# 사내 API 서버 주소 (격리망 내부). 미설정 시 "/api" 로 요청합니다.
VITE_API_BASE_URL=/api

# 데모용 목(mock) 백엔드 사용 여부.
# - 개발/데모: 미설정 또는 true → axios 어댑터가 응답을 대신합니다(백엔드 불필요).
# - 실서비스: false → 실제 HTTP 요청이 VITE_API_BASE_URL 로 나갑니다.
VITE_ENABLE_MOCK=true
